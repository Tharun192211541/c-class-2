#include <stdio.h>
#include <string.h>
 
int main()
{
   char string[1000];
 
   printf("Input a string to convert to reverse\n");
   gets(string);
 
   printf("Input string in reverse: %s\n",strrev(string));
 
   return  0;
}
